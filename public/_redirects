# Cloudflare Pages SPA fallback
# Static files excluded in _routes.json are served directly by Cloudflare
# All other routes fall through to index.html for client-side routing
/*    /index.html   200
