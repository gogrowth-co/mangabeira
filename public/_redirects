# Explicitly serve static files first to prevent SPA catchall from interfering
/sitemap.xml    /sitemap.xml    200
/robots.txt     /robots.txt     200
/llms.txt       /llms.txt       200
/llms-full.txt  /llms-full.txt  200

# Serve all other routes through the SPA
/*    /index.html   200
