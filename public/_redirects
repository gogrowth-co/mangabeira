# Static files - serve directly (MUST come before SPA catch-all)
/sitemap.xml    /sitemap.xml    200
/robots.txt     /robots.txt     200
/favicon.ico    /favicon.ico    200

# SPA fallback - all other routes go through React Router
/*    /index.html   200
